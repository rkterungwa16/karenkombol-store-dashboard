export enum ClientRoutes {
  SIGNUP = "/signup",
  LOGIN = "/login",
  DASHBOARD = "/dashboard",
  PRODUCT = "/products",
  ROLES = "/roles",
  USERS = "/users",
}

export enum ProductClientRoutes {
  PRODUCT_CATEGORIES = "{products}/categories",
  PRODUCT_COLLECTIONS = "{products}/collections",
  PRODUCT_SHIRTS = "{products}/shirts",
  PRODUCT_DRESSES = "{products}/dresses",
}
